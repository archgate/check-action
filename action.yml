name: "Archgate Check"
description: "Run Archgate ADR compliance checks with GitHub annotations for violations."
author: "Archgate"
branding:
  icon: "check-circle"
  color: "blue"

inputs:
  version:
    description: "Archgate version to install (e.g. v0.14.0). Defaults to latest."
    required: false
    default: ""

runs:
  using: "composite"
  steps:
    - name: Setup Archgate
      uses: archgate/setup-action@b59adbe1ed404916ee60e1d558766718d5e3076c # v1
      with:
        version: ${{ inputs.version }}

    - name: Run Archgate Check
      shell: bash
      run: |
        archgate check --output github
